## Copy to `.env` and fill values

# Required for OpenRouter access
OPENROUTER_API_KEY=

# Optional overrides (defaults work if unset)
#OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional: you can force a specific scorer model different from the assistant
# list by setting this (otherwise the scorer defaults to the assistant model):
#OPENROUTER_SCORER_MODEL=openai/gpt-oss-20b:free
# Rate limits are now configured per-model in data/models.json
# OpenRouter free models allow 20 requests/minute (default: 0.367 RPS = 22 RPM, burst 5)
# Concurrency is configured per-model in data/models.json

# Benchmark runner tuning (defaults below)
#BENCH_TIMEOUT_S=60
#BENCH_MAX_RETRIES=3
